# Runtime Python dependencies for Retraction Contagion Graph
# Install with: pip install -r requirements.txt
#
# All version pins use "~=" (compatible-release operator) so patch-level
# updates install automatically while major API breaks are blocked.

# --------------------------------------------------------------------------
# HTTP / networking
# --------------------------------------------------------------------------
requests~=2.32          # CSV download, Crossref & Semantic Scholar API calls

# --------------------------------------------------------------------------
# Data validation & settings
# --------------------------------------------------------------------------
pydantic~=2.10          # All data models (shared/schemas.py)
pydantic-settings~=2.14 # Settings from .env (shared/config.py)

# --------------------------------------------------------------------------
# API server
# --------------------------------------------------------------------------
fastapi~=0.131          # HTTP routing (api/main.py)
uvicorn[standard]~=0.41 # ASGI server (includes WebSocket support + watch extras)

# --------------------------------------------------------------------------
# Cognee (memory layer)
# --------------------------------------------------------------------------
# cognee is the core dependency — version constraint kept broad because
# the service layer (memory/cognee_service.py) negotiates the runtime API
# surface at first use and supports both pre-1.0 and post-1.0 Cognee.
cognee>=0.1.0

# --------------------------------------------------------------------------
# Development & testing (optional — these are NOT needed to run the demo)
# --------------------------------------------------------------------------
# Uncomment the block below if you are developing / running tests:
#
# pytest~=8.0
# pytest-asyncio~=0.23
# httpx~=0.28     # async HTTP client used in FastAPI test client
# ruff~=0.4       # linter/formatter
# mypy~=1.10      # static type checker
